//checkboxes and checking function
//adding to each category individually
import { useEditItemMutation } from "../storage/pantryPartyApi"

export default function GroceryFunctions() {

    return (<div></div>)
}

export function handleCheck(itemId, event) {
    // fn to change item's inPantry status to TRUE when clicked on grocery list
    // useEditItemMutation 
    // send item.id and event

    // body: JSON.stringify({
    //     inPantry: event })
    return (<></>)
}

export function addToCategory(category){
    //category parameter taken from groceryList map
    return (<></>)
}